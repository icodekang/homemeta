# HomeMeta 软件架构设计文档

## 1. 项目概述

### 1.1 项目背景

| 项目 | 规格 |
|------|------|
| 硬件平台 | RK3588 (ARMv8.2, 6核 Cortex-A76/A55, 6TOPS NPU) |
| 视频输入 | 6路 1080P RTSP 流 |
| AI 模型 | YOLO v8 (目标检测) + YOLO-Pose (行为识别) |
| 处理帧率 | 1 fps 降采样 |
| 应用场景 | 家庭安防监控、智能分析、机器人联动 |

### 1.2 设计目标

- **高性能**: 充分利用 RK3588 NPU 加速 AI 推理
- **低延迟**: 6路视频实时分析与告警
- **低功耗**: 针对嵌入式设备优化
- **可扩展**: 模块化设计，支持功能扩展
- **稳定性**: 7×24 小时稳定运行
- **互联互通**: 支持家庭机器人与外部设备联动

---

## 2. 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           应用层 (Application Layer)                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │  Web UI    │  │  REST API   │  │  WebSocket │  │  告警通知   │    │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘    │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
┌─────────────────────────────────────────────────────────────────────────┐
│                          业务层 (Business Layer)                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │  设备管理   │  │  任务调度   │  │  告警管理   │  │  录像管理   │    │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘    │
│  ┌─────────────────────────────┐  ┌─────────────────────────────────┐   │
│  │    机器人联动模块           │  │    外部设备接入模块              │   │
│  │  (Robot Integration)        │  │  (Device Integration)           │   │
│  └─────────────────────────────┘  └─────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
┌─────────────────────────────────────────────────────────────────────────┐
│                          处理层 (Processing Layer)                       │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                    AI 推理引擎 (Inference Engine)                 │   │
│  │  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐       │   │
│  │  │  YOLO v8      │  │  YOLO-Pose    │  │  后处理       │       │   │
│  │  │  目标检测     │  │  行为识别     │  │  NMS/追踪     │       │   │
│  │  └───────────────┘  └───────────────┘  └───────────────┘       │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                    │                                    │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                    视频处理管道 (Video Pipeline)                  │   │
│  │  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐       │   │
│  │  │  RTSP 解码    │  │  降采样       │  │  预处理       │       │   │
│  │  │  (6路并行)    │  │  1fps        │  │  归一化       │       │   │
│  │  └───────────────┘  └───────────────┘  └───────────────┘       │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
┌─────────────────────────────────────────────────────────────────────────┐
│                          基础设施层 (Infrastructure Layer)               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │  Media SDK  │  │  NPU 运行时 │  │  消息队列   │  │  存储引擎   │    │
│  │  (RGA/VPU)  │  │  (RKNN)     │  │  (ZeroMQ)   │  │  (SQLite)   │    │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘    │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────────────┐ │
│  │  协议接入层     │  │  MQTT Broker    │  │  HTTP/WS Client         │ │
│  │  (Protocol Hub)│  │  (EMQX/Mosquitto)│ │                         │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
┌─────────────────────────────────────────────────────────────────────────┐
│                          硬件抽象层 (HAL)                                │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │    NPU     │  │    VPU     │  │    GPU     │  │    DDR     │    │
│  │  (6TOPS)   │  │  (8K解码)   │  │  (Mali)    │  │  (8GB)     │    │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘    │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 2.1 扩展架构图 - 机器人联动与外部设备接入

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        HomeMeta 智能家居平台                              │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                      外部设备接入层                               │   │
│  │  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐   │   │
│  │  │  MQTT     │  │  REST     │  │ WebSocket │  │  CoAP     │   │   │
│  │  │  客户端   │  │  客户端   │  │  客户端   │  │  客户端   │   │   │
│  │  └─────┬─────┘  └─────┬─────┘  └─────┬─────┘  └─────┬─────┘   │   │
│  │        │              │              │              │         │   │
│  │        └──────────────┴──────────────┴──────────────┘         │   │
│  │                               │                                 │   │
│  │                    ┌──────────▼──────────┐                     │   │
│  │                    │   协议适配层        │                     │   │
│  │                    │  (Protocol Adapter)│                     │   │
│  │                    └──────────┬──────────┘                     │   │
│  └────────────────────────────────┼────────────────────────────────┘   │
│                                   │                                     │
│  ┌────────────────────────────────┼────────────────────────────────┐   │
│  │                        机器人联动层                              │   │
│  │                                                                  │   │
│  │  ┌─────────────────┐  ┌─────────────────┐  ┌───────────────┐   │   │
│  │  │   机器人网关    │  │   动作编排      │  │   状态同步   │   │   │
│  │  │  (Robot Gateway)│ │  (Action Orch)  │  │  (State Sync) │   │   │
│  │  └────────┬────────┘  └────────┬────────┘  └──────┬──────┘   │   │
│  │           │                      │                   │         │   │
│  │  ┌────────┴────────┐  ┌──────────┴─────────┐  ┌──────┴──────┐   │   │
│  │  │  瓦利机器人     │  │  其他智能设备       │  │  传感器阵列  │   │   │
│  │  │  (WALL-E)      │  │  (Smart Devices)   │  │  (Sensors)  │   │   │
│  │  └─────────────────┘  └─────────────────────┘  └────────────┘   │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                   │                                     │
│                                   ▼                                     │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                      事件总线 (Event Bus)                        │   │
│  │           ZeroMQ / Redis Pub-Sub / MQTT Broker                  │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 3. 各模块职责

### 3.1 视频采集模块 (Video Capture)

| 职责 | 说明 |
|------|------|
| RTSP 连接管理 | 维护 6 路 RTSP 连接，支持断线重连 |
| 视频解码 | 利用 RK3588 VPU 硬件解码 (H.264/H.265) |
| 帧提取 | 1fps 降采样控制 |
| 错误处理 | 网络异常、格式错误的容错处理 |

**技术要点**:
- 使用 MediaMTX 或自定义 RTSP Client
- VPU 硬件解码降低 CPU 占用
- 双缓冲机制避免帧丢失

### 3.2 AI 推理引擎 (Inference Engine)

| 职责 | 说明 |
|------|------|
| 模型加载 | YOLO v8 + YOLO-Pose 模型管理 |
| NPU 加速 | 利用 RK3588 NPU 进行推理加速 |
| 批处理 | 多路视频批量推理提高效率 |
| 结果后处理 | NMS、置信度过滤、坐标转换 |

**技术要点**:
- RKNN (RK3588 NPU SDK) 加速推理
- INT8 量化模型减少内存和延迟
- 批处理提升 NPU 利用率

### 3.3 行为识别模块 (Behavior Recognition)

| 职责 | 说明 |
|------|------|
| 姿态估计 | 人体 17 关键点检测 |
| 行为分析 | 跌倒、徘徊、闯入等行为判断 |
| 时序分析 | 基于多帧的行为序列分析 |
| 告警触发 | 异常行为实时告警 |

### 3.4 任务调度模块 (Task Scheduler)

| 职责 |说明 |
|------|------|
| 帧率控制 | 1fps 精确控制 |
| 负载均衡 | 6 路视频负载分配 |
| 优先级队列 | 告警帧优先处理 |
| 资源管理 | CPU/NPU/内存资源分配 |

### 3.5 存储模块 (Storage)

| 职责 | 说明 |
|------|------|
| 事件存储 | 告警事件持久化 (SQLite) |
| 视频片段 | 告警前后录像存储 |
| 模型缓存 | AI 模型文件管理 |
| 日志存储 | 运行日志和统计 |

### 3.6 通信模块 (Communication)

| 职责 | 说明 |
|------|------|
| REST API | 设备配置、状态查询 |
| WebSocket | 实时告警推送 |
| 消息队列 | 模块间异步通信 |

### 3.7 机器人联动模块 (Robot Integration)

| 职责 | 说明 |
|------|------|
| 机器人网关 | 统一接入家庭机器人（如瓦利 WALL-E） |
| 动作编排 | 根据告警事件触发机器人特定动作 |
| 状态同步 | 实时同步机器人状态与家居状态 |
| 语音交互 | 机器人语音播报与语音控制 |

**技术要点**:
- 机器人适配器模式，支持多种机器人协议
- 动作模板库，预定义告警对应的机器人响应
- 双向通信，机器人状态实时回传

### 3.8 外部设备接入模块 (Device Integration)

| 职责 | 说明 |
|------|------|
| 协议适配层 | 统一处理 MQTT、REST、WebSocket、CoAP 等协议 |
| 设备发现 | 自动发现局域网内的智能设备 |
| 数据转换 | 统一设备数据格式 |
| 状态管理 | 设备在线状态与属性管理 |

**支持的协议**:

| 协议 | 用途 | 特点 |
|------|------|------|
| MQTT | 设备控制与状态同步 | 轻量级、发布/订阅模式 |
| REST | 设备配置与查询 | 简单易用、HTTP 接口 |
| WebSocket | 实时双向通信 | 低延迟、推送支持 |
| CoAP | 物联网设备 | 低功耗、UDP 支持 |

---

## 4. 技术选型

### 4.1 编程语言

| 组件 | 语言 | 理由 |
|------|------|------|
| 核心服务 | Python 3.11 | 丰富的 AI 生态，快速开发 |
| AI 推理 | C++ (RKNN) | NPU SDK 原生支持，性能关键 |
| 视频处理 | C++ / Python | GStreamer/MediaSDK 生态 |
| Web 服务 | Python (FastAPI) | 高性能异步 Web 框架 |

### 4.2 框架与库

| 类别 | 选型 | 版本 |
|------|------|------|
| AI 框架 | Ultralytics YOLO | v8.x |
| NPU 推理 | RKNN Toolkit 2 | v2.x |
| 视频处理 | GStreamer | v1.22+ |
| Web 框架 | FastAPI | v0.109+ |
| 消息队列 | ZeroMQ | v6.x |
| 数据库 | SQLite3 / Redis | - |
| 异步编程 | asyncio | Python 3.11+ |
| 容器化 | Docker | v24+ |

### 4.3 模型优化

| 优化项 | 方法 | 预期收益 |
|--------|------|----------|
| 量化 | INT8 量化 | 推理速度 2-3x |
| 剪枝 | 结构化剪枝 | 模型体积减少 30% |
| 融合 | 算子融合 | 减少内存访问 |
| 批处理 | 多帧批处理 | NPU 利用率提升 |

---

## 5. 数据流设计

### 5.1 视频处理主流程

```
RTSP Stream (Camera 1~6)
        │
        ▼
┌───────────────────┐
│   RTSP Client     │  ← 6路并发连接
│   (视频解码)       │  ← VPU 硬件加速
└───────────────────┘
        │
        ▼ (1fps 降采样)
┌───────────────────┐
│   Frame Buffer    │  ← 双缓冲队列
│   (帧缓冲队列)     │
└───────────────────┘
        │
        ▼
┌───────────────────┐
│   Preprocessor   │  ← 图像预处理
│   (图像预处理)     │  ← 归一化、Resize
└───────────────────┘
        │
        ▼
┌───────────────────┐
│   NPU Inference   │  ← RKNN 加速
│   (AI 推理)        │  ← YOLO v8 + Pose
└───────────────────┘
        │
        ▼
┌───────────────────┐
│   Post-processor  │  ← NMS、过滤
│   (后处理)        │  ← 目标追踪
└───────────────────┘
        │
        ▼
┌───────────────────┐
│   Behavior Analysis│  ← 行为判断
│   (行为分析)       │  ← 告警生成
└───────────────────┘
        │
        ▼
┌───────────────────┐
│   Alert Manager   │  ← 告警通知
│   (告警管理)       │  ← WebSocket推送
└───────────────────┘
```

### 5.2 模块间通信

```
┌─────────┐     ZeroMQ      ┌─────────┐     ZeroMQ      ┌─────────┐
│ Video   │ ──────────────▶ │  AI     │ ──────────────▶ │ Alert  │
│ Capture │    Frame Queue  │ Engine  │   Result Queue  │ Manager│
└─────────┘                 └─────────┘                 └─────────┘
       │                         │                         │
       │                         │                         │
       ▼                         ▼                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Redis (共享状态)                            │
│  - 设备在线状态    - 当前告警    - 统计指标    - 配置缓存       │
└─────────────────────────────────────────────────────────────────┘
```

### 5.3 告警数据流

```
AI 检测结果
     │
     ▼
┌──────────────┐    否    ┌──────────────┐
│  行为规则    │─────────▶│  忽略        │
│  匹配?       │
└──────────────┘
     │ 是
     ▼
┌──────────────┐
│  告警生成    │
│  - 时间戳    │
│  - 通道      │
│  - 类型      │
│  - 置信度    │
└──────────────┘
     │
     ├──────────────────┬──────────────────┐
     ▼                  ▼                  ▼
┌─────────┐      ┌─────────┐      ┌─────────┐
│ SQLite  │      │WebSocket│      │ 通知    │
│ 存储    │      │ 推送    │      │ 服务    │
└─────────┘      └─────────┘      └─────────┘
```

---

## 6. 关键设计决策

### 6.1 为什么选择 1fps 降采样?

| 考虑因素 | 决策 |
|----------|------|
| 家庭场景特点 | 无需实时 30fps，1fps 足够检测异常 |
| NPU 负载 | 6 路 1080P @ 1fps 约需 15-20 TOPS (NPU 6TOPS 可处理) |
| 存储成本 | 视频存储空间减少 30 倍 |
| 告警准确性 | 降低误报率，提高检测质量 |

### 6.2 NPU 加速策略

**核心策略**: 利用 RK3588 NPU 进行 AI 推理加速

1. **模型量化**: FP16 → INT8 量化，推理速度提升 2-3 倍
2. **算子优化**: 使用 RKNN 优化算子 (Conv, Pooling, Softmax)
3. **内存复用**: NPU 与 DDR 直接数据传输，减少 CPU 拷贝
4. **批处理**: 多路视频批量推理，提高 NPU 利用率

### 6.3 并行架构设计

```
                    ┌─────────────┐
                    │  Main       │
                    │  Process    │
                    └──────┬──────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ▼                  ▼                  ▼
┌───────────────┐  ┌───────────────┐  ┌───────────────┐
│   Camera 1    │  │   Camera 2    │  │   Camera N    │
│   Handler     │  │   Handler     │  │   Handler     │
│   (Thread)    │  │   (Thread)    │  │   (Thread)    │
└───────────────┘  └───────────────┘  └───────────────┘
        │                  │                  │
        └──────────────────┼──────────────────┘
                           │
                           ▼
                ┌─────────────────────┐
                │   AI Inference      │
                │   Pool (Process)     │
                │   - Worker 1         │
                │   - Worker 2         │
                │   - Worker N         │
                └─────────────────────┘
```

**设计理由**:
- 每路视频独立线程，避免单路故障影响全局
- AI 推理使用进程池，避免 GIL 限制
- 6 路视频共用推理资源，提高 NPU 利用率

### 6.4 内存管理策略

| 策略 | 实现 |
|------|------|
| 内存池 | 预分配帧缓冲区，避免动态分配 |
| 零拷贝 | NPU 直接读取 DDR，减少数据拷贝 |
| 显存优化 | INT8 模型降低显存占用 |
| 缓存管理 | 模型缓存常驻，帧缓冲循环使用 |

### 6.5 容错与恢复

| 故障场景 | 恢复策略 |
|----------|----------|
| RTSP 断连 | 指数退避重连 (1s, 2s, 4s, 8s...) |
| NPU 异常 | 自动降级到 CPU 推理 (RKNPU fallback) |
| 内存泄漏 | 定期内存回收，进程健康检查 |
| 程序崩溃 | Watchdog 监控，自动重启 |

---

## 7. 性能指标

### 7.1 预期性能

| 指标 | 目标值 |
|------|--------|
| 视频延迟 | < 500ms (从采集到告警) |
| CPU 占用 | < 40% (6 核负载) |
| NPU 利用率 | > 70% |
| 内存占用 | < 4GB |
| 告警准确率 | > 95% |

### 7.2 资源需求

| 资源 | 需求 |
|------|------|
| RK3588 | 6 核 + 8GB RAM + NPU |
| 存储 | 64GB+ SSD (系统 + 录像) |
| 网络 | 100Mbps+ |
| 电源 | 12V/2A DC |

---

## 8. 目录结构

```
homemeta/
├── docs/                    # 文档
│   └── architecture.md      # 本文档
├── src/
│   ├── core/               # 核心模块
│   │   ├── __init__.py
│   │   ├── config.py       # 配置管理
│   │   ├── logger.py       # 日志
│   │   └── exceptions.py   # 异常定义
│   ├── video/              # 视频采集
│   │   ├── capture.py      # RTSP 采集
│   │   ├── decoder.py      # 视频解码
│   │   └── pipeline.py     # 视频管道
│   ├── ai/                 # AI 推理
│   │   ├── engine.py       # 推理引擎
│   │   ├── yolov8.py       # YOLO v8 检测
│   │   ├── pose.py         # 姿态估计
│   │   └── postprocess.py  # 后处理
│   ├── behavior/           # 行为分析
│   │   ├── analyzer.py     # 行为分析器
│   │   ├── detector.py     # 异常检测
│   │   └── alerts.py       # 告警管理
│   ├── storage/            # 存储
│   │   ├── database.py     # SQLite
│   │   ├── cache.py        # Redis
│   │   └── recorder.py     # 录像
│   ├── api/                # Web API
│   │   ├── main.py         # FastAPI
│   │   ├── routes/         # 路由
│   │   └── websocket.py    # WebSocket
│   ├── robot/              # 机器人联动
│   │   ├── gateway.py      # 机器人网关
│   │   ├── adapter.py      # 机器人适配器
│   │   ├── orchestrator.py # 动作编排
│   │   └── state_sync.py   # 状态同步
│   ├── device/             # 外部设备接入
│   │   ├── protocol_hub.py # 协议中心
│   │   ├── mqtt_client.py  # MQTT 客户端
│   │   ├── rest_client.py  # REST 客户端
│   │   ├── ws_client.py    # WebSocket 客户端
│   │   ├── discovery.py    # 设备发现
│   │   └── converter.py    # 数据转换
│   └── main.py             # 入口
├── models/                 # AI 模型
│   ├── yolov8n-int8.rknn
│   └── yolov8s-pose-int8.rknn
├── config/                 # 配置文件
│   └── config.yaml
├── tests/                  # 测试
├── docker/                 # Docker
├── requirements.txt
├── pyproject.toml
└── README.md
```

---

## 9. 机器人联动设计

### 9.1 机器人网关架构

```
┌─────────────────────────────────────────────────────────────────┐
│                      机器人网关 (Robot Gateway)                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐      │
│  │   瓦利适配器  │    │  通用适配器   │    │  自定义适配器 │      │
│  │  (WALL-E)    │    │  (Universal) │    │  (Custom)    │      │
│  └──────┬───────┘    └──────┬───────┘    └──────┬───────┘      │
│         │                   │                   │              │
│         └───────────────────┼───────────────────┘              │
│                             │                                    │
│                    ┌────────▼────────┐                          │
│                    │   适配器管理器  │                          │
│                    │ (Adapter Mgr)   │                          │
│                    └────────┬────────┘                          │
│                             │                                    │
│                    ┌────────▼────────┐                          │
│                    │   统一接口层    │                          │
│                    │  (Robot API)    │                          │
│                    └────────┬────────┘                          │
│                             │                                    │
└─────────────────────────────┼────────────────────────────────────┘
                              │
                    ┌─────────▼─────────┐
                    │   事件总线        │
                    │  (ZeroMQ/Redis)  │
                    └───────────────────┘
```

### 9.2 支持的机器人类型

| 机器人类型 | 通信协议 | 支持功能 |
|------------|----------|----------|
| 瓦利 (WALL-E) | HTTP REST / WebSocket | 动作执行、状态查询、语音播报 |
| 通用机器人 | MQTT / REST | 动作触发、状态同步 |
| 自定义机器人 | WebSocket / HTTP | 可扩展接口 |

### 9.3 动作编排规则

```
告警事件 ──▶ 动作匹配引擎 ──▶ 机器人动作执行

示例场景:
┌──────────────┬────────────────────┬────────────────────────┐
│   告警类型   │    触发条件         │       机器人动作        │
├──────────────┼────────────────────┼────────────────────────┤
│ 陌生人闯入   │ 置信度 > 0.8        │ 移动到门口 + 语音警告   │
│ 跌倒检测     │ 姿态评估为跌倒      │ 移动到身边 + 询问       │
│ 异常声响     │ 声音识别为异常     │ 移动到声源 + 拍照       │
│ 紧急求助     │ 用户触发            │ 立即响应 + 通知家人     │
└──────────────┴────────────────────┴────────────────────────┘
```

---

## 10. 外部设备接入设计

### 10.1 协议适配层架构

```
┌─────────────────────────────────────────────────────────────────┐
│                     协议适配层 (Protocol Adapter)                │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                      设备注册中心                         │   │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐    │   │
│  │  │ 设备ID  │  │ 设备类型│  │ 协议类型│  │ 在线状态│    │   │
│  │  └─────────┘  └─────────┘  └─────────┘  └─────────┘    │   │
│  └──────────────────────────────────────────────────────────┘   │
│                              │                                   │
│  ┌─────────────┐  ┌─────────┴────────┐  ┌─────────────────┐   │
│  │  MQTT       │  │    REST          │  │   WebSocket     │   │
│  │  协议栈     │  │    协议栈        │  │    协议栈       │   │
│  │             │  │                  │  │                 │   │
│  │ - 发布/订阅 │  │ - GET/POST/PUT   │  │ - 双向通信      │   │
│  │ - QoS      │  │ - 认证           │  │ - 心跳          │   │
│  │ - 遗嘱消息 │  │ - SSL/TLS        │  │ - 重连          │   │
│  └──────┬──────┘  └────────┬─────────┘  └────────┬────────┘   │
│         │                  │                     │            │
│         └──────────────────┼─────────────────────┘            │
│                            │                                    │
│                    ┌───────▼───────┐                          │
│                    │  统一消息格式  │                          │
│                    │  (Unified Msg)│                          │
│                    └───────────────┘                          │
└─────────────────────────────────────────────────────────────────┘
```

### 10.2 MQTT 集成

| 功能 | 说明 |
|------|------|
| 连接管理 | 自动重连、心跳保活 |
| 主题订阅 | 支持通配符订阅 (`home/+/status`) |
| 消息路由 | 主题到处理器的映射 |
| QoS 支持 | QoS 0/1/2 |

**主题规范**:
```
home/{device_id}/status     # 设备状态发布
home/{device_id}/command    # 设备命令下发
home/{device_id}/telemetry  # 设备数据上报
home/alert/{type}           # 告警通知
```

### 10.3 REST 设备接入

| 功能 | 说明 |
|------|------|
| 设备发现 | mDNS/SSDP 局域网发现 |
| 状态查询 | GET /device/{id}/state |
| 控制命令 | POST /device/{id}/action |
| 批量操作 | POST /devices/batch |

### 10.4 WebSocket 实时通信

| 功能 | 说明 |
|------|------|
| 长连接 | 双向实时通信 |
| 心跳 | 30s  ping/pong |
| 重连 | 指数退避重连 |
| 消息队列 | 离线消息缓存 |

---

## 11. 总结

本架构针对 RK3588 硬件平台特性进行了深度优化，并扩展了机器人联动与外部设备接入能力:

1. **NPU 优先**: 充分利用 6TOPS NPU 加速 AI 推理
2. **并行处理**: 6 路视频独立采集，共享 AI 推理资源
3. **1fps 平衡**: 在性能和准确性之间取得最佳平衡
4. **模块化设计**: 清晰的层次结构，便于维护和扩展
5. **容错机制**: 完善的故障恢复，保证稳定运行
6. **机器人联动**: 支持家庭机器人（如瓦利）动作编排与状态同步
7. **设备互联**: 统一的协议适配层，支持 MQTT/REST/WebSocket 主流协议

本设计可支撑 6 路 1080P 视频的实时 AI 分析，并预留了扩展接口支持更多路数或更复杂的 AI 场景，同时支持与家庭机器人和智能设备的无缝集成。
